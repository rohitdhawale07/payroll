// No TypeScript interfaces are needed here, as JSX doesn't require them

// You can directly use objects or props as needed in your JSX components

const LoginCredentials = {
  email: '',
  password: ''
};

const AuthState = {
  isAuthenticated: false,
  user: {
    email: '',
    role: ''
  }
};
